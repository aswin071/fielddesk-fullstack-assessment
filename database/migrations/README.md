# Database Migrations

Django migration files live with their owning backend apps. This directory is reserved for database-level operational notes or reviewed SQL that cannot be represented safely through Django migrations.

